machine:
  xcode:
    version: "8.2"
test:
  override:
    - xctool test -project 'xcodeProject/ObjectiveC.xcodeproj' -scheme 'xobjectivecTest' -reporter json-compilation-database:compile_commands.json  -reporter plain:plain-output.txt -reporter pretty
